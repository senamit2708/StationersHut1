

 exception i found


 1.  Null pointer Exception on .setOnClickListener

--its because of on button  findviewbyId(R.id.butoon_submit);
i used some othere id here..from another class...thats why this error occured.

2. Caused by: android.content.res.Resources$NotFoundException: String resource ID #0x0

---  int num =  cursor.getCount();
           txtCountNumber.setText(num);  --writting this this error occured (heretxtCountNumeber is TexView txtCountNUmber)

      the problem is solver like this
      productCount.setText("The total num is" + num);

      I dont know why



 why i want content provider in my app:

 You want to implement the AbstractThreadedSyncAdapter, CursorAdapter, or CursorLoader classes.

attribute name of content provider

To avoid conflicts, authority names should use a Java-style naming convention (such as com.example.provider.cartoonprovider). Typically, it's the name of the ContentProvider subclass that implements the provider
There is no default.


ContentUris.parseId(uri)
parseId
https://developer.android.com/reference/android/content/ContentUris.html#parseId(android.net.Uri)

added in API level 1
long parseId (Uri contentUri)
Converts the last path segment to a long.




 Integer productPrice = contentValues.getAsInteger(ProductDesriptionEntry.COLUMN_PRODUCT_PRICE);
        //when i used int here...null value condition its not accepting
        if (productPrice<0 || productPrice==null){
            throw new IllegalArgumentException("Product price is empty");
        }

The java string valueOf() method converts different types of values into string. By the help of string valueOf() method, you can convert int to string, long to string, boolean to string, character to string, float to string, double to string, object to string and char array to string.
  txtProductName.setText(productName);
        txtProductPrice.setText(String.valueOf(productPrice));


     public CursorLoader(Context context, Uri uri, String[] projection, String selection, String[] selectionArgs, String sortOrder) {
           super((Context)null);
           throw new RuntimeException("Stub!");
       }


java.lang.NullPointerException: Attempt to invoke virtual method 'void android.widget.EditText.setText(java.lang.CharSequence)' on a null object reference

occurs because EditText edit1;
edit1.setText("dfd");
here edit1 is not defined thats why


android.content.res.Resources$NotFoundException: String resource ID #0xf
never ever store any data except String in editText or Textview
u can covert it like this

int productPrice = cursor.getInt(indexProductPrice);
   edtProductPrice.setText(Integer.toString(productPrice));


once i insert button into listview onItemClick listener is not working

in button enter these
android:focusable="false"
        android:focusableInTouchMode="false"